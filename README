
  MTG (Multi-trait GREML and GBLUP program) 
  SH Lee (Jul/2014)

  mtg-f95    : compiled with gfortran
  mtg        : compiled with intel ifort (possible to parallel e.g. -thread k)
  rtmx_frq-vx: estimate mean(x) and var(x) (x is 0,1,2 SNP coefficient)
  snp_blup1.5: converting individual BLUP to SNP BLUP or the otherway around

  NOTE: when scaling x, we use var(x) rather than 2p(1-p) (p is RAF)


  <2 traits model>

  ./mtg-f95 -p toy.fam -d toy.phen -g toy.rtmx -out toy.2t.ascm -mod 2 -bv toy.2t.bv -cove 1

  - To get SNP BLUP for the first trait,
  awk '$1==1 {print $2}' toy.2t.bv.py > tmp
  ./snp_blup1.5 toy tmp a toy.2t.snpv

  - To get individual BLUP for the data 'toy' that can be replced with any validation set 
  ./snp_blup1.5 toy toy.2t.snpv b toy.2t.gbv

  This gives the same values between trait 1 in toy.2t.bv and toy.2t.gbv



  <3 traits model>

  ./mtg-f95 -p toy.fam -d toy.phen -g toy.rtmx -out toy.3t.ascm -mod 3 -bv toy.3t.bv -cove 1

  - To get SNP BLUP for the third trait,
  awk '$1==3 {print $2}' toy.3t.bv.py > tmp
  ./snp_blup1.5 toy tmp a toy.3t.snpv

  - To get individual BLUP for the data 'toy' that can be replced with any validation set 
  ./snp_blup1.5 toy toy.3t.snpv b toy.3t.gbv

  This gives the same values between trait 3 in toy.3t.bv and toy.3t.gbv



 

